'use client';

import { Flexbox, Text } from '@lobehub/ui';
import { memo } from 'react';

const AdminSessionsPage = memo(() => (
  <Flexbox>
    <Text style={{ fontSize: 24 }}>会话存档</Text>
  </Flexbox>
));

export default AdminSessionsPage;
