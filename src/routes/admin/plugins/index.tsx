'use client';

import { Flexbox, Text } from '@lobehub/ui';
import { memo } from 'react';

const AdminPluginsPage = memo(() => (
  <Flexbox>
    <Text style={{ fontSize: 24 }}>插件管理</Text>
  </Flexbox>
));

export default AdminPluginsPage;
