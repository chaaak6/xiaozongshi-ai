'use client';

import { Flexbox, Text } from '@lobehub/ui';
import { memo } from 'react';

const AdminRbacPage = memo(() => (
  <Flexbox>
    <Text style={{ fontSize: 24 }}>权限管理</Text>
  </Flexbox>
));

export default AdminRbacPage;
