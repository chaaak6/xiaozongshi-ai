'use client';

import { Flexbox, Text } from '@lobehub/ui';
import { memo } from 'react';

const AdminUsersPage = memo(() => (
  <Flexbox>
    <Text style={{ fontSize: 24 }}>用户管理</Text>
  </Flexbox>
));

export default AdminUsersPage;
