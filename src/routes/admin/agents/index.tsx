'use client';

import { Flexbox, Text } from '@lobehub/ui';
import { memo } from 'react';

const AdminAgentsPage = memo(() => (
  <Flexbox>
    <Text style={{ fontSize: 24 }}>智能体管理</Text>
  </Flexbox>
));

export default AdminAgentsPage;
